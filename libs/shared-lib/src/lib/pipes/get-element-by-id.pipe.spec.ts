import { GetElementByIDPipe } from './get-element-by-id.pipe';

describe('GetElementByIDPipe', () => {
  it('create an instance', () => {
    const pipe = new GetElementByIDPipe();
    expect(pipe).toBeTruthy();
  });
});
