import { FindCamelCasePipe } from './find-camel-case.pipe';

describe('FindCamelCasePipe', () => {
  it('create an instance', () => {
    const pipe = new FindCamelCasePipe();
    expect(pipe).toBeTruthy();
  });
});
